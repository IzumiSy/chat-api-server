
options: &options
  raise_not_found_error: false

default: &default
  default:
    uri: <%= ENV['MONGOLAB_URI'] %>
    options:
      user: <%= ENV['MONGOLAB_USER'] %>
      password: <%= ENV['MONGOLAB_PASSWORD'] %>

development:
  options:
    <<: *options
  clients:
    <<: *default

test:
  options:
    <<: *options
  clients:
    <<: *default

production:
  options:
    <<: *options
  clients:
    <<: *default
